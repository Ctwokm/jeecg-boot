<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.ureport.mapper.UreportMapper">

    <select id="qryReportTestList" parameterType="java.util.Map" resultType="java.util.Map">
        select r.create_time, r.username, r.sex, r.email, r.phone from sys_user r
        where 1=1
        <if test="begin_time != null" >
            and to_char(r.create_time, 'yyyy-mm-dd hh:ii:ss') >= #{begin_time}
        </if>
        <if test="begin_time != null" >
            and to_char(r.create_time, 'yyyy-mm-dd hh:ii:ss') &lt;= #{end_time}
        </if>
        order by create_time desc
    </select>

</mapper>
